# FRECKA 0.10.4 – BACKUP-001

Stand: 10. August 2026

Status: Release-Vorbereitung; noch kein Tag, kein Artefakt und keine Beta-Freigabe

Release-ID: nach dem Release-Commit gemäß `0.10.4-<kurzer-commit>`
Vorgesehener Git-Tag: `v0.10.4` (noch nicht angelegt)

## Zweck

Version 0.10.4 ist ein Backup-Workflow-Patch auf Basis von 0.10.3. Bei einem bewusst gesperrten historisch inkonsistenten Gesamtbestand endet die Sicherung weiterhin vor Verschlüsselung und Ausgabe, zeigt den Grund nun aber verständlich direkt am Formular an. Sicherungskennwörter bleiben bei Fehlern und Share-Abbruch für einen erneuten Versuch erhalten.

## Änderungen gegenüber 0.10.3

- deterministische Sequenz Snapshot → Verschlüsselung → genau eine Ausgabe;
- verständliche Sperrmeldung bei historischer Gutschein-/Beleginkonsistenz ohne interne IDs, Codes oder Stacktraces;
- Sicherungskennwörter werden nur nach erfolgreicher Ausgabe geleert;
- explizite iOS-taugliche Share-Aktion für die bereits lokal verschlüsselte Sicherungsdatei;
- kein Download-Fallback und kein zweiter Share-Aufruf nach Share-Abbruch oder technischem Dateifehler;
- gezielte Erfolgs-, Invarianten-, Verschlüsselungs-, Datei- und Share-Abbruchtests.

## Architektur und unveränderte Bereiche

- IndexedDB-Schema bleibt Version 5;
- Backupformat und kryptografisches Format bleiben Version 1;
- globale Snapshot- und Gutschein-/Beleg-Invarianten bleiben unverändert;
- keine Datenmigration oder Altbestandsreparatur;
- keine Änderung an Restore, Export, Dokument, PDF, QR oder Public Viewer;
- keine neue externe Abhängigkeit.

## Versionssignale

- Produktversion: `0.10.4`;
- Build: `BACKUP-001`;
- HTML-Titel: `FRECKA – BACKUP-001`;
- Asset-Abfragewert: `backup001-1`;
- App-Shell-Cache: `frecka-app-shell-0.10.4-backup001-1`;
- Manifestname und Apple-App-Titel: `FRECKA`;
- Datenbank-, Backup-, Export- und Public-Dokumentformatversionen bleiben unverändert.

## Einmalige SERVICEWORKER-002-Legacy-Brücke

Die seit 0.10.2 mitgeführte Legacy-Brücke bleibt in 0.10.4 unverändert aktiv, weil die dokumentierte reale Übergangsabnahme bereits ausgelieferter Altclients noch nicht bestätigt ist. Sie läuft erst nach vollständig erfolgreichem App-Shell-Download, verwendet kein `clients.claim()` und löst keinen automatischen Reload aus.

Diese Beibehaltung ist keine dauerhafte Updatepolitik. Sobald der reale Altclient-Übergang dokumentiert bestätigt wurde, ist die Entfernung der Legacy-Konstante und des automatischen Installationsaufrufs ein zwingendes Gate für den unmittelbar folgenden Worker/Release. Danach gilt ausschließlich: Update-Hinweis → bewusste Nutzeraktion → `SKIP_WAITING` → genau ein Reload.

## Upgrade und Rückweg

Beim Upgrade von 0.10.3 wird ausschließlich die neue statische App-Shell geladen. IndexedDB-Schema und gespeicherte Geschäftsdaten bleiben unverändert. Ein technischer Rückweg auf das unveränderliche Release `0.10.3-3591b0b` ist formatseitig möglich, stellt jedoch den dortigen lautlosen Backup-Fehlerpfad wieder her und repariert keine historischen Daten.

## Lokaler Prüfnachweis vor Tag und Artefakt

- JavaScript-Syntax aller Laufzeit- und Testdateien: bestanden;
- Manifest-JSON: bestanden;
- nativer Browser-Smoke-Test mit 144 Fällen einschließlich BACKUP-001, PERSISTENCE-007, Backup, Restore und Export: bestanden, 0 fehlgeschlagen;
- PWA-Update- und Service-Worker-Tests einschließlich Legacy-Brücke, Nutzeraktivierung, genau einem Reload, Release-Unterpfad, Offline-Fallback und Cache-Isolation: bestanden;
- Public-Viewer-Boot ohne Unternehmer-IndexedDB: bestanden (`IndexedDB.open 0`);
- PDF-Rendering und QR-Größenmessung: bestanden;
- lokaler DEPLOY-006-Sicherheits-Smoke-Test ohne Netzwerktransfer: bestanden;
- dokumentierte Vendor-Prüfsummen: bestanden;
- 320 px und 390 px ohne horizontalen Seitenüberlauf: bestanden;
- sichtbare Produktversion `0.10.4` und Buildkennung `BACKUP-001`: bestanden;
- keine Browser-Konsolenfehler: bestanden;
- `git diff --check`: bestanden.

Dieser lokale Nachweis ersetzt weder den vorgesehenen Tag noch die Abnahme eines später erzeugten unveränderlichen Artefakts auf einem realen iPhone beziehungsweise bestehenden Home-Screen-PWA-Client.

## Freigabegrenze

Version 0.10.4 besitzt in diesem Stand weder ein Beta-GO noch eine Produktivfreigabe. Commit, Tag, Release-Artefakt, Deployment und reale iPhone-Abnahme bleiben eigene, ausdrücklich freizugebende Schritte.
